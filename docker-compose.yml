version: '3'
services:
    web:
        image: nginx:latest
        ports:
            - "8087:80"
        volumes:
            - ./conf.d:/etc/nginx/conf.d
            - ./app:/app
        depends_on:
            - php
        restart: always
    php:
        build:
            context: .
            dockerfile: PHP.Dockerfile
            args:
                APP_ENV: ${APP_ENV}
        volumes:
            - ./app:/app
            - ./uploads.ini:/usr/local/etc/php/conf.d/uploads.ini
        depends_on:
            - mysql
        restart: always
    mysql:
        image: mariadb:latest
        environment:
            MYSQL_ROOT_PASSWORD: ${DB_PASSWORD}
            MYSQL_USER: ${DB_USER}
            MYSQL_PASSWORD: ${DB_PASSWORD}
            MYSQL_DATABASE: ${DB_NAME}
        volumes:
            - photovaliant_phpcms:/var/lib/mysql
            - ./my_custom.cnf:/etc/mysql/mariadb.conf.d/my_custom.cnf:ro
            - photovaliant_mysql_logs:/var/log/mysql
        ports:
            - "127.0.0.1:33097:3306"
        restart: always
volumes:
    photovaliant_phpcms: {}
    photovaliant_mysql_logs: {}
        
